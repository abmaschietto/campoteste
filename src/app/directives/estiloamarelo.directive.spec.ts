import { EstiloamareloDirective } from './estiloamarelo.directive';

describe('EstiloamareloDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloamareloDirective();
    expect(directive).toBeTruthy();
  });
});
