import { SizedirectiveDirective } from './sizedirective.directive';

describe('SizedirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SizedirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
